# Application
APP_NAME=Unified JIRA-Confluence Backend
APP_VERSION=0.1.0
APP_ENV=development

# Server
# The app listens on APP_PORT; if your platform sets PORT it will be mapped automatically by run.sh.
APP_HOST=0.0.0.0
APP_PORT=3001

# CORS
# Comma-separated list of allowed origins, or "*" to allow all (no credentials).
CORS_ORIGINS=*

# Atlassian OAuth 2.0 (Authorization Code) - REQUIRED
# Create an app in https://developer.atlassian.com/console/ and configure the redirect URI EXACTLY.
ATLASSIAN_CLIENT_ID=your_client_id
ATLASSIAN_CLIENT_SECRET=your_client_secret
ATLASSIAN_REDIRECT_URI=http://localhost:3001/api/v1/auth/atlassian/callback

# Optional: Preconfigure base URLs for your Atlassian cloud sites.
# If not provided, the backend will attempt to discover accessible resources using the access token.
# Example: https://api.atlassian.com/ex/jira/{cloudid}/rest/api/3
JIRA_BASE_URL=
# Example: https://api.atlassian.com/ex/confluence/{cloudid}/wiki/rest/api
CONFLUENCE_BASE_URL=

# Demo/dev only: In-memory token slot for quick testing (not for production).
# Leave empty; tokens are obtained via OAuth flow and held in memory until process restarts.
# ACCESS_TOKEN and REFRESH_TOKEN are not read from env at runtime for security best practices.
