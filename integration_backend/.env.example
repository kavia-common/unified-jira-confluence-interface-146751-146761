# Example environment for integration_backend (FastAPI)
# Copy to .env and adjust values as needed.

# Application
APP_NAME=Unified JIRA-Confluence Backend
APP_VERSION=0.1.0
APP_ENV=development

# Networking
# Orchestrator expects backend on 3001 by default.
# APP_PORT is preferred; PORT will be mapped to APP_PORT by run.sh if APP_PORT is not set.
APP_HOST=0.0.0.0
APP_PORT=3001

# CORS
# Comma-separated list of origins, or "*" to allow all (non-credentialed).
CORS_ORIGINS=*

# Integrations (placeholders)
JIRA_BASE_URL=
CONFLUENCE_BASE_URL=
